const initialState = [
  {
    company: "Kweller Prep",
    filename: "kweller.jpg",
    url: '/ad/kweller'
  }
];

const reducer = (state = { ...initialState }) => {
  return state;
};

export default reducer;
